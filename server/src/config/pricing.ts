// Placeholder flat rate — real distance/weight-based shipping cost comes
// from the ShippingProvider in the Shiprocket phase.
// Temporarily waived (set to 0) — re-enable by restoring 4900 once ready.
export const FLAT_DELIVERY_FEE = 0;
